# Dependencies
/node_modules

# Next.js
/.next/
/out/
.swc/
tsconfig.tsbuildinfo

# Local env files — never commit these
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.vercel

# Telegram scraper state
.telegram-session
.telegram-state.json
.telegram-scout-state.json

# Script byproducts, not source: generated data manifests, and .backups/,
# which holds gzipped DB dumps. The tools themselves are tracked.
/scripts/*.json
/scripts/.backups/
data/tweet-queue.json

# Regenerable renders — node brand-assets/generate-infographics.mjs
# and node scripts/wir-figure.mjs render --week <week>
brand-assets/0*.png
brand-assets/wir/wir-????-??-??.png

# Radix source checkouts cloned on demand by the radix skill
.repos

# Local agent config and notes
CLAUDE.md
.claude/skills/

# Misc
.DS_Store
*.pem
*.log
*.csv

# Generated by Next on every build
next-env.d.ts
